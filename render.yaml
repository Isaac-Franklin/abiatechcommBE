services:
  - type: web
    name: django-backend
    runtime: python
    buildCommand: ./build.sh
    startCommand: gunicorn abiatechcomm_BE.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
